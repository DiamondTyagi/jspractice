/*const balance = 1000;
if(balance < 1368) console.log("less that require balance"),
console.log("can't sign-in");*/
//short hand implementation of the if-statement.
const userloggedin = true;
const debitcarddetails = true;
if(userloggedin && debitcarddetails && 2 == 3){
    console.log("user is signed in");
    
}